enum UserType {
  ADMIN,
  USER
}

type User {
  id: ID! @id
  email: String! @unique
  password: String!
  name: String!
  surname: String!
  address: String
  phone: String! @unique
  type: UserType @default(value: USER)
}
